<h1>Listing retros</h1>

<table>
  <tr>
    <th>Team</th>
    <th>Comment type</th>
    <th>Comment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @retros.each do |retro| %>
  <tr>
    <td><%= retro.team_id %></td>
    <td><%= retro.comment_type %></td>
    <td><%= retro.comment %></td>
    <td><%= link_to 'Show', retro %></td>
    <td><%= link_to 'Edit', edit_retro_path(retro) %></td>
    <td><%= link_to 'Destroy', retro, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Retro', new_retro_path %>
